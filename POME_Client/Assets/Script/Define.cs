using UnityEngine;
using System.Collections;

namespace Define
{
    //public enum Type { NONE, RED, ORANGE, YELLOW, GREEN, BLUE, INDIGO, VIOLET };
    public enum Type { NONE, RED, YELLOW, BLUE };

    public enum STATE { NONE, INIT, IDLE, SELLECT };
}
